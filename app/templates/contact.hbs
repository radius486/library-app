<h1>Contact Page</h1>
<p class="well well-sm">If you have any question or feedback please leave a message with your email address.</p>
<div class="row">
  <div class="col-md-6">
    <div class={{model.emailFieldClass}}>
      <label>Your email address*:</label>
      {{input type="email" class="form-control" placeholder="Please type your e-mail address." value=model.email}}
      {{#if model.isEmailValid}}
        <span class="glyphicon glyphicon-ok form-control-feedback" aria-hidden="true"></span>
      {{/if}}
      </div>
    <div class={{model.messageFieldClass}}>
      <label>Your message*:</label>
      {{textarea class="form-control" placeholder="Your message. (At least 5 characters.)" value=model.message rows="7"}}
      {{#if model.isMessageValid}}
        <span class="glyphicon glyphicon-ok form-control-feedback" aria-hidden="true"></span>
      {{/if}}
    </div>
    <div class="form-group">
      <button class="btn btn-success" disabled={{model.isDisabled}} {{action 'sendMessage' model}}>Send message</button>
    </div>
  </div>
</div>
{{#if model.responseMessage}}
  <div class="alert alert-success">{{model.responseMessage}}</div>
{{/if}}
